{
  "name": "photon-mixer",
  "version": "0.1.0",
  "description": "Digital-native illustration software with float32 linear color and GPU-native architecture",
  "type": "module",
  "main": "dist/electron/main.js",
  "scripts": {
    "build": "tsc && powershell -Command \"Copy-Item -Path shaders -Destination dist -Recurse -Force; Copy-Item -Path electron/preload.cjs -Destination dist/electron -Force\"",
    "dev": "tsc && powershell -Command \"Copy-Item -Path shaders -Destination dist -Recurse -Force; Copy-Item -Path electron/preload.cjs -Destination dist/electron -Force\" && electron .",
    "watch": "tsc --watch",
    "test": "node --import tsx --test tests/*.test.ts",
    "test:watch": "node --import tsx --test --watch tests/*.test.ts"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "@webgpu/types": "^0.1.70",
    "electron": "^30.0.0",
    "tsx": "^4.22.4",
    "typescript": "^5.4.0"
  },
  "dependencies": {
    "fflate": "^0.8.3",
    "lit": "^3.3.3",
    "playwright-core": "^1.60.0"
  }
}
